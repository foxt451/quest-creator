const queryNames = {
  QUESTS: "Quests",
  QUEST: "Quest",
  ADD_QUEST: "AddQuest",
  UPDATE_QUEST: "UpdateQuest",
} as const;

const inputTypeNames = {
  QUEST_DATA: "QuestData",
};

export { queryNames, inputTypeNames };
